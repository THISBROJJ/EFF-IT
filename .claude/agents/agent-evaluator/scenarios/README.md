# scenarios/

Novel inputs, edge cases, or surprising behavior the `agent-evaluator` has encountered.
One scenario per file: `<brief-slug>.md` — what the input was, what happened, why it's noteworthy.
