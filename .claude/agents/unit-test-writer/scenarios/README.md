# scenarios/

Novel inputs, edge cases, or surprising behavior the `unit-test-writer` has encountered.
One scenario per file: `<brief-slug>.md` — what the input was, what happened, why it's noteworthy.
