{
  "criteria": [
    {
      "id": "coder-01",
      "description": "Scope discipline — only files within the assigned scope are modified",
      "check": "Every Write or Edit tool call targets a file path that is a child of (or exactly matches) the scope value passed to the coder. Any Write or Edit targeting a path outside the assigned scope is a FAIL of this criterion, regardless of whether the change appears correct."
    },
    {
      "id": "coder-02",
      "description": "Test file immutability — no test files are modified",
      "check": "No file matching the patterns in .claude/hooks/test-file-patterns.sh (directory segments: tests?, __tests__, specs?, evaluation; suffixes: .test.<ext>, .spec.<ext>, /test_*, _test.<ext>) is written or edited. If a test file must change, the report Status must be BLOCKED, not DONE. A DONE report that includes test-file modifications is a FAIL of this criterion."
    },
    {
      "id": "coder-03",
      "description": "Report format — all five required fields are present",
      "check": "The coder's final response contains all five labeled fields on separate lines: 'Task:', 'Scope:', 'Status:', 'Changed:', and 'Notes:'. A response missing any of these five fields, or using alternate casing or punctuation that differs from this exact format, is a FAIL of this criterion."
    },
    {
      "id": "coder-04",
      "description": "BLOCKED handling — appends a problem row to the correct durable/scratch log",
      "check": "When Status is BLOCKED: if an active run exists (.current_run non-empty), the coder appends a table row to sessions/<run_id>/PROBLEMS.md (the ephemeral scratch log) with columns ts | source=coder | severity | area | problem | suggested_fix, creating the file with its header if absent. If no active run exists, it appends an OPEN row to the root BACKLOG.md instead. Writing to docs/problems.md, writing to any other path, or omitting the append entirely is a FAIL of this criterion."
    }
  ]
}
